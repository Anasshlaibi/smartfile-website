<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # توجيه أي طلب مباشرة لمجلد public بلا ماتبان فالرابط
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>